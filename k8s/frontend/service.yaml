apiVersion: v1
kind: Service
metadata:
  name: pl-stats-frontend-svc
  labels:
    app: pl-stats-frontend
spec:
  selector:
    app: pl-stats-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer