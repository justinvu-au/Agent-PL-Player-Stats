apiVersion: v1
kind: Service
metadata:
  name: pl-stats-agent-svc
  labels:
    app: pl-stats-agent
spec:
  selector:
    app: pl-stats-agent
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP