apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pl-stats-agent-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: pl-stats-agent
  endpoints:
    - port: http
      path: /metrics
      interval: 15s